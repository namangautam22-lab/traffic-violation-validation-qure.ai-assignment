// HelperStrip is no longer needed as primary nav —
// keyboard hints are inlined in the queue page and navigation bar.
// Keeping as a no-op export to avoid breaking any remaining imports.

export function HelperStrip() {
  return null;
}
